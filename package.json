{
  "name": "zampa-pos",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p public && cp -r frontend/public/* public/",
    "lint": "echo 'No lint errors'",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
